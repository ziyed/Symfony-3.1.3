{% extends 'base.html.twig' %}

{% block body %}
    <h1>Edit Role</h1>
    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input  class="btn btn-success" type="submit" value="Edit" />
    {{ form_end(edit_form) }}
    <br/>    
    <br/>    
    <a class="btn btn-success" href="{{ path('roles_index') }}">Back to the list</a>    
{% endblock %}
